import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'superjogos.io.app',
  appName: 'superjogos',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
